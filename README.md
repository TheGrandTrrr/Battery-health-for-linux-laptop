# **INSTALLATION**
To clone this script using git, run this command in your teminal *(git required ofc)*:
```shell
git clone https://github.com/TheGrandTrrr/Battery-health-for-linux-laptop.git && cd Battery-health-for-linux-laptop
```
From this repo run this command:
```shell
sudo bash ./install.sh
```
# **USAGE**
to use this script just run `bthlt` in your terminal

# ***DONE.***
use it nicely, babe <3
